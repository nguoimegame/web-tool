---
import FileHashPage from '../../components/FileHashPage.astro';
import { useTranslations } from '../../i18n/utils';

const t = useTranslations('en');
const title = t('sha384_file_hash.title');
const description = t('sha384_file_hash.description');
const algorithmName = "SHA384";
const jsFile = "sha384.min.js";
const activePage = "hash/sha384_file_hash";
---
<FileHashPage 
    title={title}
    description={description}
    algorithmName={algorithmName}
    jsFile={jsFile}
    activePage={activePage}
/>
